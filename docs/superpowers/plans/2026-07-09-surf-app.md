# Surf App Pays Basque v1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a private Android APK that shows Pays Basque surf spots on a color-coded map with a /100 score, powered by embedded spot data (scraped once), Open-Meteo Marine, and SHOM tides.

**Architecture:** npm workspaces monorepo — `packages/scoring` (pure TS algo), `scripts/scrape` (Cheerio → SQLite), `apps/mobile` (Expo app calling APIs on launch). Static `spots.db` bundled in the APK; no backend.

**Tech Stack:** Expo SDK 52, TypeScript, expo-sqlite, react-native-maps, react-native-gifted-charts, Vitest, Cheerio, better-sqlite3 (scrape script only), EAS Build

---

## File Map

| Path | Responsibility |
|---|---|
| `package.json` | npm workspaces root |
| `packages/scoring/src/types.ts` | Shared interfaces (`Spot`, `HourlyConditions`, `ScoreBreakdown`) |
| `packages/scoring/src/angles.ts` | `angleDiff`, `isInAngleWindow` on 0–360° circle |
| `packages/scoring/src/swell.ts` | Houle sub-score (period, height, direction multiplier) |
| `packages/scoring/src/wind.ts` | Vent sub-score + `MalusVent` |
| `packages/scoring/src/tide.ts` | Marée sub-score + `computeTideStage` |
| `packages/scoring/src/index.ts` | `computeSurfScore` orchestrator |
| `packages/scoring/src/index.test.ts` | Hendaye / optimal / flat regression tests |
| `scripts/scrape/spots-seed.json` | 12 spots: slug, name, lat, lon, URL overrides |
| `scripts/scrape/parsers/surf-forecast.ts` | Parse Best wind / Best swell from HTML |
| `scripts/scrape/parsers/wannasurf.ts` | Parse Spot characteristics block |
| `scripts/scrape/parsers/surf-report.ts` | Parse French description + tide hints |
| `scripts/scrape/parsers/surf-forecast.test.ts` | Snapshot tests against fixture HTML |
| `scripts/scrape/build-db.ts` | Merge parsers → `data/spots.db` |
| `scripts/scrape/fixtures/*.html` | Committed HTML snapshots for CI |
| `apps/mobile/app.json` | Expo config |
| `apps/mobile/eas.json` | APK preview profile |
| `apps/mobile/assets/spots.db` | Copied from `data/spots.db` at build time |
| `apps/mobile/src/api/open-meteo.ts` | Marine forecast client |
| `apps/mobile/src/api/shom.ts` | Tide heights + stage computation |
| `apps/mobile/src/db/spots.ts` | Load spots from expo-sqlite |
| `apps/mobile/src/hooks/useSurfConditions.ts` | Fetch + score all spots |
| `apps/mobile/src/screens/MapScreen.tsx` | Main map + refresh |
| `apps/mobile/src/components/SpotMarker.tsx` | Colored pin with score |
| `apps/mobile/src/components/SpotDetailSheet.tsx` | Bottom sheet detail |
| `apps/mobile/src/components/HourlyScoreChart.tsx` | Daily score curve |
| `apps/mobile/src/components/NetworkError.tsx` | Full-screen offline state |
| `apps/mobile/src/utils/score-color.ts` | green / orange / red / grey thresholds |

---

### Task 1: Monorepo scaffold

**Files:**
- Create: `package.json`
- Create: `.gitignore`
- Create: `tsconfig.base.json`

- [ ] **Step 1: Create root package.json**

```json
{
  "name": "app-surf",
  "private": true,
  "workspaces": ["packages/*", "apps/*", "scripts/scrape"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "scrape": "npm run build-db --workspace=scripts-scrape",
    "copy-db": "cp data/spots.db apps/mobile/assets/spots.db"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
```

- [ ] **Step 2: Create .gitignore**

```
node_modules/
data/spots.db
apps/mobile/assets/spots.db
.env
.env.local
dist/
.expo/
*.apk
```

- [ ] **Step 3: Create tsconfig.base.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

- [ ] **Step 4: Install dependencies**

Run: `npm install`
Expected: `node_modules/` created, no errors

- [ ] **Step 5: Commit**

```bash
git add package.json .gitignore tsconfig.base.json
git commit -m "chore: scaffold npm workspaces monorepo"
```

---

### Task 2: Scoring package — types and setup

**Files:**
- Create: `packages/scoring/package.json`
- Create: `packages/scoring/tsconfig.json`
- Create: `packages/scoring/vitest.config.ts`
- Create: `packages/scoring/src/types.ts`

- [ ] **Step 1: Create packages/scoring/package.json**

```json
{
  "name": "@app-surf/scoring",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
```

- [ ] **Step 2: Create packages/scoring/tsconfig.json**

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src"]
}
```

- [ ] **Step 3: Create packages/scoring/vitest.config.ts**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: { environment: 'node' },
});
```

- [ ] **Step 4: Create packages/scoring/src/types.ts**

```typescript
export type TideStage = 'low' | 'mid-rising' | 'mid-falling' | 'high';

export interface Spot {
  spotId: string;
  name: string;
  slug: string;
  latitude: number;
  longitude: number;
  beachOrientation: number;
  swellAngleMin: number;
  swellAngleMax: number;
  windOffshoreMin: number;
  windOffshoreMax: number;
  idealSwellHeightMin: number;
  idealSwellHeightMax: number;
  tideOptimalStage: TideStage;
  bottomType?: string;
  level?: string;
  descriptionFr?: string;
}

export interface HourlyConditions {
  waveHeight: number;       // meters
  wavePeriod: number;       // seconds
  waveDirection: number;    // degrees
  windSpeedKnots: number;
  windDirection: number;    // degrees
  tideStage: TideStage;
}

export interface ScoreBreakdown {
  swellScore: number;
  windScore: number;
  tideScore: number;
  windMalus: number;
  saturationMalus: number;
  total: number;
  windLabel: 'Glassy' | 'Offshore' | 'Cross-shore' | 'Onshore';
}
```

- [ ] **Step 5: Install workspace deps**

Run: `npm install`
Expected: workspace `@app-surf/scoring` linked

- [ ] **Step 6: Commit**

```bash
git add packages/scoring/
git commit -m "feat(scoring): add package scaffold and types"
```

---

### Task 3: Angle utilities

**Files:**
- Create: `packages/scoring/src/angles.ts`
- Create: `packages/scoring/src/angles.test.ts`

- [ ] **Step 1: Write the failing test**

```typescript
// packages/scoring/src/angles.test.ts
import { describe, it, expect } from 'vitest';
import { angleDiff, isInAngleWindow, distanceOutsideWindow } from './angles';

describe('angleDiff', () => {
  it('returns shortest arc between two bearings', () => {
    expect(angleDiff(350, 10)).toBe(20);
    expect(angleDiff(10, 350)).toBe(20);
    expect(angleDiff(150, 310)).toBe(160);
  });
});

describe('isInAngleWindow', () => {
  it('handles windows that do not cross 0', () => {
    expect(isInAngleWindow(300, 290, 320)).toBe(true);
    expect(isInAngleWindow(270, 290, 320)).toBe(false);
  });
});

describe('distanceOutsideWindow', () => {
  it('returns 0 when inside window', () => {
    expect(distanceOutsideWindow(300, 290, 320)).toBe(0);
  });
  it('returns degrees when outside', () => {
    expect(distanceOutsideWindow(270, 290, 320)).toBe(20);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test --workspace=@app-surf/scoring`
Expected: FAIL — module `./angles` not found

- [ ] **Step 3: Implement angles.ts**

```typescript
// packages/scoring/src/angles.ts
export function angleDiff(a: number, b: number): number {
  const diff = Math.abs(a - b) % 360;
  return diff > 180 ? 360 - diff : diff;
}

export function isInAngleWindow(angle: number, min: number, max: number): boolean {
  const n = ((angle % 360) + 360) % 360;
  const a = ((min % 360) + 360) % 360;
  const b = ((max % 360) + 360) % 360;
  if (a <= b) return n >= a && n <= b;
  return n >= a || n <= b;
}

export function distanceOutsideWindow(angle: number, min: number, max: number): number {
  if (isInAngleWindow(angle, min, max)) return 0;
  const n = ((angle % 360) + 360) % 360;
  const a = ((min % 360) + 360) % 360;
  const b = ((max % 360) + 360) % 360;
  const distToA = angleDiff(n, a);
  const distToB = angleDiff(n, b);
  return Math.min(distToA, distToB);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test --workspace=@app-surf/scoring`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add packages/scoring/src/angles.ts packages/scoring/src/angles.test.ts
git commit -m "feat(scoring): add angle utilities"
```

---

### Task 4: Swell score

**Files:**
- Create: `packages/scoring/src/swell.ts`
- Create: `packages/scoring/src/swell.test.ts`

- [ ] **Step 1: Write the failing test**

```typescript
import { describe, it, expect } from 'vitest';
import { computeSwellScore } from './swell';
import type { Spot } from './types';

const hendayeSpot: Pick<Spot,
  'swellAngleMin' | 'swellAngleMax' | 'idealSwellHeightMin' | 'idealSwellHeightMax'
> = {
  swellAngleMin: 290,
  swellAngleMax: 320,
  idealSwellHeightMin: 0.8,
  idealSwellHeightMax: 1.8,
};

describe('computeSwellScore', () => {
  it('scores Hendaye swell at 25 (270° is 20° outside window)', () => {
    const result = computeSwellScore(hendayeSpot, {
      waveHeight: 1.2,
      wavePeriod: 11,
      waveDirection: 270,
    });
    expect(result).toBe(25); // (25+25)*0.5
  });

  it('returns 0 for flat conditions', () => {
    const result = computeSwellScore(hendayeSpot, {
      waveHeight: 0.2,
      wavePeriod: 5,
      waveDirection: 300,
    });
    expect(result).toBe(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test --workspace=@app-surf/scoring -- src/swell.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement swell.ts**

```typescript
import { distanceOutsideWindow } from './angles';
import type { Spot } from './types';

interface SwellInput {
  waveHeight: number;
  wavePeriod: number;
  waveDirection: number;
}

function scorePeriod(period: number): number {
  if (period < 7) return 2;
  if (period < 10) return 12;
  if (period < 14) return 25;
  return 20;
}

function scoreHeight(
  height: number,
  min: number,
  max: number,
): number {
  if (height < 0.4) return 0;
  if (height >= min && height <= max) return 25;
  if (height > max) return 5;
  return 12; // below ideal but surfable
}

function swellDirectionMultiplier(
  direction: number,
  min: number,
  max: number,
): number {
  const outside = distanceOutsideWindow(direction, min, max);
  if (outside === 0) return 1.0;
  if (outside <= 20) return 0.5;
  return 0.0;
}

export function computeSwellScore(
  spot: Pick<Spot, 'swellAngleMin' | 'swellAngleMax' | 'idealSwellHeightMin' | 'idealSwellHeightMax'>,
  input: SwellInput,
): number {
  const periodPts = scorePeriod(input.wavePeriod);
  const heightPts = scoreHeight(
    input.waveHeight,
    spot.idealSwellHeightMin,
    spot.idealSwellHeightMax,
  );
  const mult = swellDirectionMultiplier(
    input.waveDirection,
    spot.swellAngleMin,
    spot.swellAngleMax,
  );
  return (periodPts + heightPts) * mult;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test --workspace=@app-surf/scoring -- src/swell.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/scoring/src/swell.ts packages/scoring/src/swell.test.ts
git commit -m "feat(scoring): add swell score computation"
```

---

### Task 5: Wind score and malus

**Files:**
- Create: `packages/scoring/src/wind.ts`
- Create: `packages/scoring/src/wind.test.ts`

- [ ] **Step 1: Write the failing test**

```typescript
import { describe, it, expect } from 'vitest';
import { computeWindScore, computeWindMalus } from './wind';

const hendayeWind = {
  windOffshoreMin: 130,
  windOffshoreMax: 180,
};

describe('computeWindScore', () => {
  it('returns 30 for glassy (<5 kn)', () => {
    expect(computeWindScore(hendayeWind, 3, 310)).toEqual({ score: 30, label: 'Glassy' });
  });

  it('returns 0 for onshore Hendaye (310° vs offshore ~155°)', () => {
    expect(computeWindScore(hendayeWind, 14, 310)).toEqual({ score: 0, label: 'Onshore' });
  });
});

describe('computeWindMalus', () => {
  it('applies 0.3 malus for onshore >12 kn', () => {
    expect(computeWindMalus('Onshore', 14)).toBe(0.3);
  });
  it('applies 0.0 malus for onshore >20 kn', () => {
    expect(computeWindMalus('Onshore', 22)).toBe(0.0);
  });
  it('returns 1.0 otherwise', () => {
    expect(computeWindMalus('Offshore', 14)).toBe(1.0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test --workspace=@app-surf/scoring -- src/wind.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement wind.ts**

```typescript
import { angleDiff } from './angles';
import type { ScoreBreakdown } from './types';

type WindLabel = ScoreBreakdown['windLabel'];

function offshoreCenter(min: number, max: number): number {
  if (min <= max) return (min + max) / 2;
  return ((min + max + 360) / 2) % 360;
}

export function computeWindScore(
  spot: { windOffshoreMin: number; windOffshoreMax: number },
  windSpeedKnots: number,
  windDirection: number,
): { score: number; label: WindLabel } {
  if (windSpeedKnots < 5) {
    return { score: 30, label: 'Glassy' };
  }
  const thetaOffshore = offshoreCenter(spot.windOffshoreMin, spot.windOffshoreMax);
  const delta = angleDiff(windDirection, thetaOffshore);

  if (delta <= 30) return { score: 30, label: 'Offshore' };
  if (delta <= 75) return { score: 15, label: 'Cross-shore' };
  return { score: 0, label: 'Onshore' };
}

export function computeWindMalus(label: WindLabel, windSpeedKnots: number): number {
  if (label !== 'Onshore') return 1.0;
  if (windSpeedKnots > 20) return 0.0;
  if (windSpeedKnots > 12) return 0.3;
  return 1.0;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test --workspace=@app-surf/scoring -- src/wind.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/scoring/src/wind.ts packages/scoring/src/wind.test.ts
git commit -m "feat(scoring): add wind score and malus"
```

---

### Task 6: Tide score and stage computation

**Files:**
- Create: `packages/scoring/src/tide.ts`
- Create: `packages/scoring/src/tide.test.ts`

- [ ] **Step 1: Write the failing test**

```typescript
import { describe, it, expect } from 'vitest';
import { computeTideScore, computeTideStage } from './tide';

describe('computeTideStage', () => {
  const events = [
    { time: new Date('2026-07-09T06:00:00+02:00'), type: 'low' as const },
    { time: new Date('2026-07-09T12:30:00+02:00'), type: 'high' as const },
    { time: new Date('2026-07-09T18:45:00+02:00'), type: 'low' as const },
  ];

  it('returns low within 1h of BM', () => {
    const at = new Date('2026-07-09T06:30:00+02:00');
    expect(computeTideStage(at, events)).toBe('low');
  });

  it('returns mid-rising between BM and PM', () => {
    const at = new Date('2026-07-09T10:00:00+02:00');
    expect(computeTideStage(at, events)).toBe('mid-rising');
  });
});

describe('computeTideScore', () => {
  it('returns 20 for perfect match', () => {
    expect(computeTideScore('mid-rising', 'mid-rising')).toBe(20);
  });
  it('returns 10 for adjacent stage', () => {
    expect(computeTideScore('mid-rising', 'high')).toBe(10);
  });
  it('returns 2 for bad match', () => {
    expect(computeTideScore('low', 'high')).toBe(2);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test --workspace=@app-surf/scoring -- src/tide.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement tide.ts**

```typescript
import type { TideStage } from './types';

export interface TideEvent {
  time: Date;
  type: 'low' | 'high';
}

const ONE_HOUR_MS = 60 * 60 * 1000;

const ADJACENT: Record<TideStage, TideStage[]> = {
  low: ['mid-rising', 'mid-falling'],
  high: ['mid-rising', 'mid-falling'],
  'mid-rising': ['low', 'high'],
  'mid-falling': ['low', 'high'],
};

export function computeTideStage(at: Date, events: TideEvent[]): TideStage {
  const sorted = [...events].sort((a, b) => a.time.getTime() - b.time.getTime());

  for (const event of sorted) {
    if (Math.abs(at.getTime() - event.time.getTime()) <= ONE_HOUR_MS) {
      return event.type;
    }
  }

  const before = sorted.filter((e) => e.time <= at);
  const after = sorted.filter((e) => e.time > at);
  const prev = before[before.length - 1];
  const next = after[0];

  if (!prev || !next) return 'mid-rising';

  if (prev.type === 'low' && next.type === 'high') return 'mid-rising';
  if (prev.type === 'high' && next.type === 'low') return 'mid-falling';

  return 'mid-rising';
}

export function computeTideScore(actual: TideStage, optimal: TideStage): number {
  if (actual === optimal) return 20;
  if (ADJACENT[optimal].includes(actual)) return 10;
  return 2;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test --workspace=@app-surf/scoring -- src/tide.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/scoring/src/tide.ts packages/scoring/src/tide.test.ts
git commit -m "feat(scoring): add tide stage and score"
```

---

### Task 7: Full score orchestrator + Hendaye regression

**Files:**
- Create: `packages/scoring/src/index.ts`
- Create: `packages/scoring/src/index.test.ts`

- [ ] **Step 1: Write the failing Hendaye regression test**

```typescript
import { describe, it, expect } from 'vitest';
import { computeSurfScore } from './index';
import type { Spot } from './types';

const hendaye: Spot = {
  spotId: 'hendaye',
  name: 'Hendaye',
  slug: 'hendaye',
  latitude: 43.38,
  longitude: -1.77,
  beachOrientation: 20,
  swellAngleMin: 290,
  swellAngleMax: 320,
  windOffshoreMin: 130,
  windOffshoreMax: 180,
  idealSwellHeightMin: 0.8,
  idealSwellHeightMax: 1.8,
  tideOptimalStage: 'mid-rising',
};

describe('computeSurfScore', () => {
  it('Hendaye regression → 14 (displayed as 15)', () => {
    const result = computeSurfScore(hendaye, {
      waveHeight: 1.2,
      wavePeriod: 11,
      waveDirection: 270,
      windSpeedKnots: 14,
      windDirection: 310,
      tideStage: 'mid-rising',
    });
    expect(result.swellScore).toBe(25);
    expect(result.windScore).toBe(0);
    expect(result.tideScore).toBe(20);
    expect(result.windMalus).toBe(0.3);
    expect(result.total).toBe(14);
  });

  it('optimal conditions → ~100', () => {
    const result = computeSurfScore(hendaye, {
      waveHeight: 1.2,
      wavePeriod: 12,
      waveDirection: 300,
      windSpeedKnots: 3,
      windDirection: 155,
      tideStage: 'mid-rising',
    });
    expect(result.total).toBeGreaterThanOrEqual(95);
  });

  it('flat onshore → ~0', () => {
    const result = computeSurfScore(hendaye, {
      waveHeight: 0.2,
      wavePeriod: 5,
      waveDirection: 90,
      windSpeedKnots: 25,
      windDirection: 310,
      tideStage: 'high',
    });
    expect(result.total).toBe(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test --workspace=@app-surf/scoring -- src/index.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement index.ts**

```typescript
import { computeSwellScore } from './swell';
import { computeWindScore, computeWindMalus } from './wind';
import { computeTideScore } from './tide';
import type { Spot, HourlyConditions, ScoreBreakdown } from './types';

export { computeTideStage } from './tide';
export type { Spot, HourlyConditions, ScoreBreakdown, TideStage } from './types';
export type { TideEvent } from './tide';

export function computeSurfScore(
  spot: Spot,
  conditions: HourlyConditions,
): ScoreBreakdown {
  const swellScore = computeSwellScore(spot, conditions);
  const { score: windScore, label: windLabel } = computeWindScore(
    spot,
    conditions.windSpeedKnots,
    conditions.windDirection,
  );
  const tideScore = computeTideScore(conditions.tideStage, spot.tideOptimalStage);
  const windMalus = computeWindMalus(windLabel, conditions.windSpeedKnots);
  const saturationMalus = 1.0;

  const raw = swellScore + windScore + tideScore;
  const total = Math.round(raw * windMalus * saturationMalus);

  return {
    swellScore,
    windScore,
    tideScore,
    windMalus,
    saturationMalus,
    total,
    windLabel,
  };
}
```

- [ ] **Step 4: Run all scoring tests**

Run: `npm run test --workspace=@app-surf/scoring`
Expected: PASS (all tests)

- [ ] **Step 5: Commit**

```bash
git add packages/scoring/src/index.ts packages/scoring/src/index.test.ts
git commit -m "feat(scoring): add computeSurfScore orchestrator with regression tests"
```

---

### Task 8: Scraper seed data

**Files:**
- Create: `scripts/scrape/package.json`
- Create: `scripts/scrape/spots-seed.json`

- [ ] **Step 1: Create scripts/scrape/package.json**

```json
{
  "name": "scripts-scrape",
  "private": true,
  "type": "module",
  "scripts": {
    "build-db": "tsx build-db.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cheerio": "^1.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "tsx": "^4.0.0",
    "vitest": "^2.0.0"
  }
}
```

- [ ] **Step 2: Create spots-seed.json with 12 Pays Basque spots**

```json
[
  { "slug": "hendaye", "name": "Hendaye", "latitude": 43.3818, "longitude": -1.7892 },
  { "slug": "sokoa", "name": "Sokoa", "latitude": 43.3765, "longitude": -1.7710 },
  { "slug": "guethary", "name": "Guéthary", "latitude": 43.4270, "longitude": -1.6130 },
  { "slug": "parlementia", "name": "Parlementia", "latitude": 43.4305, "longitude": -1.6010 },
  { "slug": "bidart", "name": "Bidart", "latitude": 43.4370, "longitude": -1.5930 },
  { "slug": "lafitenia", "name": "Lafitenia", "latitude": 43.4180, "longitude": -1.5780 },
  { "slug": "cote-des-basques", "name": "Côte des Basques", "latitude": 43.4780, "longitude": -1.5670 },
  { "slug": "grande-plage-biarritz", "name": "Grande Plage Biarritz", "latitude": 43.4830, "longitude": -1.5580 },
  { "slug": "marinella", "name": "Marinella", "latitude": 43.4950, "longitude": -1.5350 },
  { "slug": "les-corsaires", "name": "Les Corsaires", "latitude": 43.5020, "longitude": -1.5280 },
  { "slug": "belharra", "name": "Belharra", "latitude": 43.4080, "longitude": -1.6820 },
  { "slug": "ondres", "name": "Ondres", "latitude": 43.5600, "longitude": -1.4680 }
]
```

- [ ] **Step 3: Install scrape workspace deps**

Run: `npm install`
Expected: `better-sqlite3` and `cheerio` installed

- [ ] **Step 4: Commit**

```bash
git add scripts/scrape/package.json scripts/scrape/spots-seed.json
git commit -m "feat(scrape): add spots seed data for Pays Basque"
```

---

### Task 9: Surf-Forecast parser + fixture test

**Files:**
- Create: `scripts/scrape/parsers/surf-forecast.ts`
- Create: `scripts/scrape/fixtures/surf-forecast-hendaye.html` (downloaded once manually)
- Create: `scripts/scrape/parsers/surf-forecast.test.ts`
- Create: `scripts/scrape/vitest.config.ts`

- [ ] **Step 1: Download fixture HTML**

Run: `curl -sL "https://www.surf-forecast.com/breaks/Hendaye/spot-guide" -o scripts/scrape/fixtures/surf-forecast-hendaye.html`
Expected: file size > 10 KB

- [ ] **Step 2: Write the failing test**

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'fs';
import { parseSurfForecast } from './surf-forecast';

describe('parseSurfForecast', () => {
  it('extracts swell and wind angle windows from Hendaye', () => {
    const html = readFileSync('fixtures/surf-forecast-hendaye.html', 'utf-8');
    const result = parseSurfForecast(html);
    expect(result.swellAngleMin).toBeGreaterThan(0);
    expect(result.swellAngleMax).toBeGreaterThan(result.swellAngleMin);
    expect(result.windOffshoreMin).toBeGreaterThan(0);
    expect(result.windOffshoreMax).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npm run test --workspace=scripts-scrape`
Expected: FAIL

- [ ] **Step 4: Implement parser**

```typescript
// scripts/scrape/parsers/surf-forecast.ts
import * as cheerio from 'cheerio';

export interface SurfForecastData {
  swellAngleMin: number;
  swellAngleMax: number;
  windOffshoreMin: number;
  windOffshoreMax: number;
}

const COMPASS_TO_DEG: Record<string, number> = {
  N: 0, NNE: 22.5, NE: 45, ENE: 67.5,
  E: 90, ESE: 112.5, SE: 135, SSE: 157.5,
  S: 180, SSW: 202.5, SW: 225, WSW: 247.5,
  W: 270, WNW: 292.5, NW: 315, NNW: 337.5,
};

function parseCompassRange(text: string): { min: number; max: number } {
  const parts = text.toUpperCase().match(/\b(NNE|ENE|ESE|SSE|SSW|WSW|WNW|NNW|NE|SE|SW|NW|N|E|S|W)\b/g);
  if (!parts || parts.length === 0) throw new Error(`No compass dirs in: ${text}`);
  const deg = parts.map((p) => COMPASS_TO_DEG[p]);
  return { min: Math.min(...deg), max: Math.max(...deg) };
}

export function parseSurfForecast(html: string): SurfForecastData {
  const $ = cheerio.load(html);
  const body = $('body').text();

  const swellMatch = body.match(/Best swell direction[:\s]+([^\n]+)/i);
  const windMatch = body.match(/Best wind[:\s]+([^\n]+)/i);

  if (!swellMatch || !windMatch) {
    throw new Error('Could not find Best swell/wind sections');
  }

  const swell = parseCompassRange(swellMatch[1]);
  const wind = parseCompassRange(windMatch[1]);

  return {
    swellAngleMin: swell.min,
    swellAngleMax: swell.max,
    windOffshoreMin: wind.min,
    windOffshoreMax: wind.max,
  };
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npm run test --workspace=scripts-scrape`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add scripts/scrape/parsers/ scripts/scrape/fixtures/ scripts/scrape/vitest.config.ts
git commit -m "feat(scrape): add Surf-Forecast parser with fixture test"
```

---

### Task 10: Wannasurf + Surf-Report parsers

**Files:**
- Create: `scripts/scrape/parsers/wannasurf.ts`
- Create: `scripts/scrape/parsers/surf-report.ts`
- Create: `scripts/scrape/parsers/wannasurf.test.ts`
- Create: `scripts/scrape/fixtures/wannasurf-hendaye.html`
- Create: `scripts/scrape/fixtures/surf-report-hendaye.html`

- [ ] **Step 1: Download fixtures**

```bash
curl -sL "https://www.wannasurf.com/spot/Europe/France/Pays_Basque/hendaye" -o scripts/scrape/fixtures/wannasurf-hendaye.html
curl -sL "https://www.surf-report.com/meteo-surf/hendaye" -o scripts/scrape/fixtures/surf-report-hendaye.html
```

- [ ] **Step 2: Write failing tests**

```typescript
// scripts/scrape/parsers/wannasurf.test.ts
import { readFileSync } from 'fs';
import { parseWannasurf } from './wannasurf';

it('parses Hendaye characteristics', () => {
  const html = readFileSync('fixtures/wannasurf-hendaye.html', 'utf-8');
  const r = parseWannasurf(html);
  expect(r.bottomType).toBeTruthy();
  expect(['low','mid-rising','mid-falling','high']).toContain(r.tideOptimalStage);
});
```

```typescript
// scripts/scrape/parsers/surf-report.test.ts
import { readFileSync } from 'fs';
import { parseSurfReport } from './surf-report';

it('parses Hendaye French description', () => {
  const html = readFileSync('fixtures/surf-report-hendaye.html', 'utf-8');
  const r = parseSurfReport(html);
  expect(r.descriptionFr.length).toBeGreaterThan(50);
});
```

- [ ] **Step 3: Implement wannasurf.ts**

```typescript
import * as cheerio from 'cheerio';
import type { TideStage } from '@app-surf/scoring';

export interface WannasurfData {
  bottomType: string;
  level: string;
  tideOptimalStage: TideStage;
}

function normalizeTide(text: string): TideStage {
  const t = text.toLowerCase();
  if (t.includes('low') || t.includes('basse')) return 'low';
  if (t.includes('high') || t.includes('haute')) return 'high';
  if (t.includes('rising') || t.includes('montante')) return 'mid-rising';
  if (t.includes('falling') || t.includes('descendante')) return 'mid-falling';
  return 'mid-rising';
}

export function parseWannasurf(html: string): WannasurfData {
  const $ = cheerio.load(html);
  const text = $('body').text();

  const bottomMatch = text.match(/Bottom[:\s]+([^\n]+)/i);
  const levelMatch = text.match(/Level[:\s]+([^\n]+)/i);
  const tideMatch = text.match(/Tide position[:\s]+([^\n]+)/i);

  return {
    bottomType: bottomMatch?.[1]?.trim().toLowerCase() ?? 'sand',
    level: levelMatch?.[1]?.trim().toLowerCase() ?? 'intermediate',
    tideOptimalStage: normalizeTide(tideMatch?.[1] ?? 'mid'),
  };
}
```

- [ ] **Step 4: Implement surf-report.ts**

```typescript
import * as cheerio from 'cheerio';
import type { TideStage } from '@app-surf/scoring';

export interface SurfReportData {
  descriptionFr: string;
  tideOptimalStage?: TideStage;
}

export function parseSurfReport(html: string): SurfReportData {
  const $ = cheerio.load(html);
  const paragraphs = $('p')
    .map((_, el) => $(el).text().trim())
    .get()
    .filter((t) => t.length > 80);
  const descriptionFr = paragraphs[0] ?? '';

  const full = $('body').text().toLowerCase();
  let tideOptimalStage: TideStage | undefined;
  if (full.includes('mi-marée') || full.includes('mi marée')) tideOptimalStage = 'mid-rising';
  else if (full.includes('marée basse')) tideOptimalStage = 'low';
  else if (full.includes('marée haute')) tideOptimalStage = 'high';

  return { descriptionFr, tideOptimalStage };
}
```

- [ ] **Step 5: Run tests**

Run: `npm run test --workspace=scripts-scrape`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add scripts/scrape/parsers/
git commit -m "feat(scrape): add Wannasurf and Surf-Report parsers"
```

---

### Task 11: Build SQLite database from scrapers

**Files:**
- Create: `scripts/scrape/build-db.ts`
- Create: `scripts/scrape/derive.ts`

- [ ] **Step 1: Create derive.ts for computed fields**

```typescript
import type { TideStage } from '@app-surf/scoring';

const HEIGHT_BY_LEVEL: Record<string, { min: number; max: number }> = {
  beginner: { min: 0.5, max: 1.2 },
  intermediate: { min: 0.8, max: 1.8 },
  advanced: { min: 1.2, max: 3.0 },
};

export function deriveBeachOrientation(swellAngleMin: number, swellAngleMax: number): number {
  const swellCenter = (swellAngleMin + swellAngleMax) / 2;
  return (swellCenter + 180) % 360;
}

export function deriveIdealHeight(level: string) {
  return HEIGHT_BY_LEVEL[level] ?? HEIGHT_BY_LEVEL.intermediate;
}

export function pickTideStage(...candidates: (TideStage | undefined)[]): TideStage {
  return candidates.find(Boolean) as TideStage ?? 'mid-rising';
}
```

- [ ] **Step 2: Create build-db.ts**

```typescript
import Database from 'better-sqlite3';
import { readFileSync, mkdirSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';
import { v4 as uuid } from 'uuid';
import { parseSurfForecast } from './parsers/surf-forecast.js';
import { parseWannasurf } from './parsers/wannasurf.js';
import { parseSurfReport } from './parsers/surf-report.js';
import { deriveBeachOrientation, deriveIdealHeight, pickTideStage } from './derive.js';

const __dir = dirname(fileURLToPath(import.meta.url));
const root = join(__dir, '../..');
const dataDir = join(root, 'data');
mkdirSync(dataDir, { recursive: true });

interface SeedSpot { slug: string; name: string; latitude: number; longitude: number; }

const seeds: SeedSpot[] = JSON.parse(readFileSync(join(__dir, 'spots-seed.json'), 'utf-8'));

const db = new Database(join(dataDir, 'spots.db'));
db.exec(`
  CREATE TABLE IF NOT EXISTS spots (
    spot_id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    slug TEXT NOT NULL UNIQUE,
    latitude REAL NOT NULL,
    longitude REAL NOT NULL,
    beach_orientation REAL NOT NULL,
    swell_angle_min REAL NOT NULL,
    swell_angle_max REAL NOT NULL,
    wind_offshore_min REAL NOT NULL,
    wind_offshore_max REAL NOT NULL,
    ideal_swell_height_min REAL NOT NULL,
    ideal_swell_height_max REAL NOT NULL,
    tide_optimal_stage TEXT NOT NULL,
    bottom_type TEXT,
    level TEXT,
    description_fr TEXT
  );
`);
db.exec('DELETE FROM spots');

const insert = db.prepare(`
  INSERT INTO spots VALUES (
    @spot_id, @name, @slug, @latitude, @longitude,
    @beach_orientation, @swell_angle_min, @swell_angle_max,
    @wind_offshore_min, @wind_offshore_max,
    @ideal_swell_height_min, @ideal_swell_height_max,
    @tide_optimal_stage, @bottom_type, @level, @description_fr
  )
`);

for (const seed of seeds) {
  const sfSlug = seed.slug === 'cote-des-basques' ? 'Cote-des-Basques' : seed.slug;
  console.log(`Scraping ${seed.name}...`);

  const [sfRes, wsRes, srRes] = await Promise.all([
    fetch(`https://www.surf-forecast.com/breaks/${sfSlug}/spot-guide`),
    fetch(`https://www.wannasurf.com/spot/Europe/France/Pays_Basque/${seed.slug}`),
    fetch(`https://www.surf-report.com/meteo-surf/${seed.slug}`),
  ]);

  const sf = parseSurfForecast(await sfRes.text());
  const ws = parseWannasurf(await wsRes.text());
  const sr = parseSurfReport(await srRes.text());
  const heights = deriveIdealHeight(ws.level);

  insert.run({
    spot_id: uuid(),
    name: seed.name,
    slug: seed.slug,
    latitude: seed.latitude,
    longitude: seed.longitude,
    beach_orientation: deriveBeachOrientation(sf.swellAngleMin, sf.swellAngleMax),
    swell_angle_min: sf.swellAngleMin,
    swell_angle_max: sf.swellAngleMax,
    wind_offshore_min: sf.windOffshoreMin,
    wind_offshore_max: sf.windOffshoreMax,
    ideal_swell_height_min: heights.min,
    ideal_swell_height_max: heights.max,
    tide_optimal_stage: pickTideStage(sr.tideOptimalStage, ws.tideOptimalStage),
    bottom_type: ws.bottomType,
    level: ws.level,
    description_fr: sr.descriptionFr,
  });
}

console.log(`Wrote ${seeds.length} spots to data/spots.db`);
db.close();
```

- [ ] **Step 3: Run scraper (requires network)**

Run: `npm run scrape`
Expected: `Wrote 12 spots to data/spots.db`

- [ ] **Step 4: Commit scripts (not data/spots.db)**

```bash
git add scripts/scrape/build-db.ts scripts/scrape/derive.ts
git commit -m "feat(scrape): add build-db pipeline to generate spots.db"
```

---

### Task 12: Expo mobile app scaffold

**Files:**
- Create: `apps/mobile/` via Expo CLI
- Modify: `apps/mobile/package.json` — add `@app-surf/scoring` dependency

- [ ] **Step 1: Create Expo app**

Run:
```bash
cd apps && npx create-expo-app@latest mobile --template blank-typescript
```
Expected: `apps/mobile/` created

- [ ] **Step 2: Add workspace dependency in apps/mobile/package.json**

```json
"dependencies": {
  "@app-surf/scoring": "*",
  "expo-sqlite": "~15.0.0",
  "react-native-maps": "1.20.0",
  "react-native-gifted-charts": "^1.4.0",
  "react-native-svg": "15.8.0"
}
```

- [ ] **Step 3: Install deps from root**

Run: `npm install`
Expected: no peer dependency errors

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/
git commit -m "feat(mobile): scaffold Expo TypeScript app"
```

---

### Task 13: Open-Meteo API client

**Files:**
- Create: `apps/mobile/src/api/open-meteo.ts`
- Create: `apps/mobile/src/api/open-meteo.test.ts`

- [ ] **Step 1: Write types and client**

```typescript
// apps/mobile/src/api/open-meteo.ts
export interface OpenMeteoHourly {
  time: string[];
  wave_height: number[];
  wave_period: number[];
  wave_direction: number[];
  wind_speed_10m: number[];
  wind_direction_10m: number[];
}

export interface MarineForecast {
  hourly: OpenMeteoHourly;
}

const MS_TO_KNOTS = 1.944;

export async function fetchMarineForecast(
  latitude: number,
  longitude: number,
): Promise<MarineForecast> {
  const url = new URL('https://marine-api.open-meteo.com/v1/marine');
  url.searchParams.set('latitude', String(latitude));
  url.searchParams.set('longitude', String(longitude));
  url.searchParams.set(
    'hourly',
    'wave_height,wave_period,wave_direction,wind_speed_10m,wind_direction_10m',
  );
  url.searchParams.set('timezone', 'Europe/Paris');
  url.searchParams.set('forecast_days', '1');

  const res = await fetch(url.toString());
  if (!res.ok) throw new Error(`Open-Meteo error: ${res.status}`);
  return res.json();
}

export function toHourlyConditions(hourly: OpenMeteoHourly, index: number) {
  return {
    waveHeight: hourly.wave_height[index] ?? 0,
    wavePeriod: hourly.wave_period[index] ?? 0,
    waveDirection: hourly.wave_direction[index] ?? 0,
    windSpeedKnots: (hourly.wind_speed_10m[index] ?? 0) * MS_TO_KNOTS,
    windDirection: hourly.wind_direction_10m[index] ?? 0,
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add apps/mobile/src/api/open-meteo.ts
git commit -m "feat(mobile): add Open-Meteo marine API client"
```

---

### Task 14: SHOM tide API client

**Files:**
- Create: `apps/mobile/src/api/shom.ts`

- [ ] **Step 1: Implement SHOM client**

Port de référence : Saint-Jean-de-Luz (`harborCode: 66` — vérifier sur data.shom.fr lors de l'implémentation).

```typescript
// apps/mobile/src/api/shom.ts
import { computeTideStage, type TideEvent } from '@app-surf/scoring';

const SHOM_BASE = 'https://services.data.shom.fr/maree/v2';

export async function fetchTideEvents(date: Date): Promise<TideEvent[]> {
  const apiKey = process.env.EXPO_PUBLIC_SHOM_API_KEY;
  if (!apiKey) throw new Error('EXPO_PUBLIC_SHOM_API_KEY not set');

  const dateStr = date.toISOString().slice(0, 10);
  const url = `${SHOM_BASE}/hlt/66/${dateStr}?apikey=${apiKey}`;

  const res = await fetch(url);
  if (!res.ok) throw new Error(`SHOM error: ${res.status}`);
  const data = await res.json();

  return (data.heights ?? data).map((entry: { datetime: string; type: string }) => ({
    time: new Date(entry.datetime),
    type: entry.type === 'high' ? 'high' : 'low',
  }));
}

export function tideStageAt(events: TideEvent[], at: Date) {
  return computeTideStage(at, events);
}
```

- [ ] **Step 2: Create .env.example**

```
EXPO_PUBLIC_SHOM_API_KEY=your_shom_key_here
```

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/src/api/shom.ts apps/mobile/.env.example
git commit -m "feat(mobile): add SHOM tide API client"
```

---

### Task 15: SQLite spot loader

**Files:**
- Create: `apps/mobile/src/db/spots.ts`
- Create: `apps/mobile/assets/` directory
- Run: `npm run copy-db` after scrape

- [ ] **Step 1: Copy bundled database**

Run: `mkdir -p apps/mobile/assets && npm run copy-db`
Expected: `apps/mobile/assets/spots.db` exists

- [ ] **Step 2: Implement spots.ts**

```typescript
import * as SQLite from 'expo-sqlite';
import type { Spot } from '@app-surf/scoring';

function rowToSpot(row: Record<string, unknown>): Spot {
  return {
    spotId: row.spot_id as string,
    name: row.name as string,
    slug: row.slug as string,
    latitude: row.latitude as number,
    longitude: row.longitude as number,
    beachOrientation: row.beach_orientation as number,
    swellAngleMin: row.swell_angle_min as number,
    swellAngleMax: row.swell_angle_max as number,
    windOffshoreMin: row.wind_offshore_min as number,
    windOffshoreMax: row.wind_offshore_max as number,
    idealSwellHeightMin: row.ideal_swell_height_min as number,
    idealSwellHeightMax: row.ideal_swell_height_max as number,
    tideOptimalStage: row.tide_optimal_stage as Spot['tideOptimalStage'],
    bottomType: row.bottom_type as string | undefined,
    level: row.level as string | undefined,
    descriptionFr: row.description_fr as string | undefined,
  };
}

export async function loadSpots(): Promise<Spot[]> {
  const db = await SQLite.openDatabaseAsync('spots.db');
  const rows = await db.getAllAsync('SELECT * FROM spots ORDER BY name');
  return rows.map((r) => rowToSpot(r as Record<string, unknown>));
}
```

- [ ] **Step 3: Configure expo-sqlite asset copy in app.json**

```json
{
  "expo": {
    "plugins": [
      [
        "expo-sqlite",
        {
          "enableFTS": false
        }
      ]
    ]
  }
}
```

Copy `assets/spots.db` to SQLite default location on first launch via `SQLite.openDatabaseAsync` with `assetSource`.

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/src/db/spots.ts apps/mobile/app.json
git commit -m "feat(mobile): add SQLite spot loader"
```

---

### Task 16: useSurfConditions hook

**Files:**
- Create: `apps/mobile/src/hooks/useSurfConditions.ts`

- [ ] **Step 1: Implement hook**

```typescript
import { useCallback, useEffect, useState } from 'react';
import { computeSurfScore, type Spot, type ScoreBreakdown, type TideStage } from '@app-surf/scoring';
import { fetchMarineForecast, toHourlyConditions } from '../api/open-meteo';
import { fetchTideEvents, tideStageAt } from '../api/shom';
import { loadSpots } from '../db/spots';

export interface HourlyScore {
  time: string;
  score: ScoreBreakdown;
  conditions: {
    waveHeight: number;
    wavePeriod: number;
    waveDirection: number;
    windSpeedKnots: number;
    windDirection: number;
    tideStage: TideStage;
  };
}

export interface SpotConditions {
  spot: Spot;
  currentScore: ScoreBreakdown;
  hourlyScores: HourlyScore[];
  error?: string;
  tideUnavailable?: boolean;
}

export function useSurfConditions() {
  const [spots, setSpots] = useState<SpotConditions[]>([]);
  const [loading, setLoading] = useState(true);
  const [networkError, setNetworkError] = useState(false);

  const refresh = useCallback(async () => {
    setLoading(true);
    setNetworkError(false);
    try {
      const allSpots = await loadSpots();
      let tideEvents;
      let tideUnavailable = false;
      try {
        tideEvents = await fetchTideEvents(new Date());
      } catch {
        tideUnavailable = true;
      }

      const results = await Promise.all(
        allSpots.map(async (spot) => {
          try {
            const forecast = await fetchMarineForecast(spot.latitude, spot.longitude);
            const { hourly } = forecast;
            const hourlyScores: HourlyScore[] = hourly.time.map((time, i) => {
              const base = toHourlyConditions(hourly, i);
              const at = new Date(time);
              const tideStage = tideEvents
                ? tideStageAt(tideEvents, at)
                : ('mid-rising' as TideStage);
              const conditions = { ...base, tideStage };
              const score = computeSurfScore(spot, conditions);
              if (tideUnavailable) {
                score.tideScore = 0;
                score.total = Math.round(
                  (score.swellScore + score.windScore) * score.windMalus,
                );
              }
              return { time, score, conditions };
            });
            const now = new Date();
            const currentIdx = hourlyScores.findIndex((h) => new Date(h.time) >= now);
            const current = hourlyScores[Math.max(0, currentIdx)];
            return { spot, currentScore: current.score, hourlyScores, tideUnavailable };
          } catch (e) {
            return {
              spot,
              currentScore: { total: 0, swellScore: 0, windScore: 0, tideScore: 0, windMalus: 1, saturationMalus: 1, windLabel: 'Onshore' as const },
              hourlyScores: [],
              error: String(e),
            };
          }
        }),
      );
      setSpots(results);
    } catch {
      setNetworkError(true);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => { refresh(); }, [refresh]);

  return { spots, loading, networkError, refresh };
}
```

- [ ] **Step 2: Commit**

```bash
git add apps/mobile/src/hooks/useSurfConditions.ts
git commit -m "feat(mobile): add useSurfConditions hook"
```

---

### Task 17: Map screen and colored markers

**Files:**
- Create: `apps/mobile/src/utils/score-color.ts`
- Create: `apps/mobile/src/components/SpotMarker.tsx`
- Create: `apps/mobile/src/screens/MapScreen.tsx`
- Modify: `apps/mobile/App.tsx`

- [ ] **Step 1: score-color.ts**

```typescript
export function scoreColor(score: number | null): string {
  if (score === null) return '#9CA3AF';
  if (score >= 60) return '#22C55E';
  if (score >= 30) return '#F97316';
  return '#EF4444';
}
```

- [ ] **Step 2: SpotMarker.tsx**

```typescript
import { View, Text, StyleSheet } from 'react-native';
import { Marker } from 'react-native-maps';
import { scoreColor } from '../utils/score-color';

interface Props {
  latitude: number;
  longitude: number;
  name: string;
  score: number | null;
  onPress: () => void;
}

export function SpotMarker({ latitude, longitude, name, score, onPress }: Props) {
  const color = scoreColor(score);
  return (
    <Marker coordinate={{ latitude, longitude }} onPress={onPress} title={name}>
      <View style={[styles.pin, { backgroundColor: color }]}>
        <Text style={styles.score}>{score ?? '—'}</Text>
      </View>
    </Marker>
  );
}

const styles = StyleSheet.create({
  pin: { borderRadius: 20, paddingHorizontal: 8, paddingVertical: 4, minWidth: 36, alignItems: 'center' },
  score: { color: '#fff', fontWeight: '700', fontSize: 14 },
});
```

- [ ] **Step 3: MapScreen.tsx**

```typescript
import { useState } from 'react';
import { View, StyleSheet, TouchableOpacity, Text, ActivityIndicator } from 'react-native';
import MapView from 'react-native-maps';
import { SpotMarker } from '../components/SpotMarker';
import { SpotDetailSheet } from '../components/SpotDetailSheet';
import { NetworkError } from '../components/NetworkError';
import { useSurfConditions, type SpotConditions } from '../hooks/useSurfConditions';

const PAY_BASQUE_REGION = {
  latitude: 43.45,
  longitude: -1.62,
  latitudeDelta: 0.35,
  longitudeDelta: 0.35,
};

export function MapScreen() {
  const { spots, loading, networkError, refresh } = useSurfConditions();
  const [selected, setSelected] = useState<SpotConditions | null>(null);

  if (networkError) return <NetworkError onRetry={refresh} />;
  if (loading) return <ActivityIndicator style={styles.loader} size="large" />;

  return (
    <View style={styles.container}>
      <MapView style={styles.map} initialRegion={PAY_BASQUE_REGION}>
        {spots.map((s) => (
          <SpotMarker
            key={s.spot.spotId}
            latitude={s.spot.latitude}
            longitude={s.spot.longitude}
            name={s.spot.name}
            score={s.error ? null : s.currentScore.total}
            onPress={() => setSelected(s)}
          />
        ))}
      </MapView>
      <TouchableOpacity style={styles.refresh} onPress={refresh}>
        <Text style={styles.refreshText}>Actualiser</Text>
      </TouchableOpacity>
      <View style={styles.legend}>
        <Text style={styles.legendText}>≥60 Bon · 30–59 Moyen · &lt;30 Mauvais</Text>
      </View>
      {selected && <SpotDetailSheet data={selected} onClose={() => setSelected(null)} />}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1 },
  map: { flex: 1 },
  loader: { flex: 1, justifyContent: 'center' },
  refresh: { position: 'absolute', top: 48, right: 16, backgroundColor: '#fff', padding: 10, borderRadius: 8 },
  refreshText: { fontWeight: '600' },
  legend: { position: 'absolute', bottom: 24, alignSelf: 'center', backgroundColor: 'rgba(255,255,255,0.9)', padding: 8, borderRadius: 8 },
  legendText: { fontSize: 12 },
});
```

- [ ] **Step 4: Wire App.tsx**

```typescript
import { MapScreen } from './src/screens/MapScreen';
export default function App() { return <MapScreen />; }
```

- [ ] **Step 5: Commit**

```bash
git add apps/mobile/src/
git commit -m "feat(mobile): add map screen with colored spot markers"
```

---

### Task 18: Spot detail bottom sheet + hourly chart

**Files:**
- Create: `apps/mobile/src/components/SpotDetailSheet.tsx`
- Create: `apps/mobile/src/components/HourlyScoreChart.tsx`
- Create: `apps/mobile/src/components/NetworkError.tsx`

- [ ] **Step 1: NetworkError.tsx**

```typescript
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';

export function NetworkError({ onRetry }: { onRetry: () => void }) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Pas de connexion</Text>
      <Text style={styles.sub}>Connecte-toi pour voir les conditions</Text>
      <TouchableOpacity style={styles.btn} onPress={onRetry}>
        <Text style={styles.btnText}>Réessayer</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 24 },
  title: { fontSize: 20, fontWeight: '700', marginBottom: 8 },
  sub: { color: '#666', marginBottom: 24 },
  btn: { backgroundColor: '#2563EB', paddingHorizontal: 24, paddingVertical: 12, borderRadius: 8 },
  btnText: { color: '#fff', fontWeight: '600' },
});
```

- [ ] **Step 2: HourlyScoreChart.tsx** using `react-native-gifted-charts` LineChart

```typescript
import { LineChart } from 'react-native-gifted-charts';
import type { HourlyScore } from '../hooks/useSurfConditions';

export function HourlyScoreChart({ data }: { data: HourlyScore[] }) {
  const points = data.map((h) => ({
    value: h.score.total,
    label: new Date(h.time).getHours() + 'h',
  }));
  const nowHour = new Date().getHours();
  return (
    <LineChart
      data={points}
      height={160}
      spacing={28}
      initialSpacing={10}
      color="#2563EB"
      thickness={2}
      hideDataPoints={false}
      xAxisLabelTextStyle={{ fontSize: 10 }}
      showVerticalLines
      verticalLinesColor="rgba(37,99,235,0.3)"
      verticalLinesThickness={nowHour > 0 ? 1 : 0}
    />
  );
}
```

- [ ] **Step 3: SpotDetailSheet.tsx**

```typescript
import { Modal, View, Text, ScrollView, TouchableOpacity, StyleSheet } from 'react-native';
import { HourlyScoreChart } from './HourlyScoreChart';
import type { SpotConditions } from '../hooks/useSurfConditions';

export function SpotDetailSheet({ data, onClose }: { data: SpotConditions; onClose: () => void }) {
  const { spot, currentScore, hourlyScores, error, tideUnavailable } = data;

  return (
    <Modal animationType="slide" transparent>
      <View style={styles.overlay}>
        <View style={styles.sheet}>
          <TouchableOpacity onPress={onClose} style={styles.close}>
            <Text>✕</Text>
          </TouchableOpacity>
          <ScrollView>
            <Text style={styles.name}>{spot.name}</Text>
            {error ? (
              <Text style={styles.error}>Données indisponibles : {error}</Text>
            ) : (
              <>
                <Text style={styles.score}>{currentScore.total}/100</Text>
                <Text style={styles.badge}>{currentScore.windLabel}</Text>
                {tideUnavailable && <Text style={styles.warn}>Marée indisponible</Text>}
                <HourlyScoreChart data={hourlyScores} />
                {spot.descriptionFr && <Text style={styles.desc}>{spot.descriptionFr}</Text>}
                <Text style={styles.meta}>Fond : {spot.bottomType} · Niveau : {spot.level}</Text>
              </>
            )}
          </ScrollView>
        </View>
      </View>
    </Modal>
  );
}

const styles = StyleSheet.create({
  overlay: { flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(0,0,0,0.4)' },
  sheet: { backgroundColor: '#fff', borderTopLeftRadius: 16, borderTopRightRadius: 16, maxHeight: '75%', padding: 20 },
  close: { alignSelf: 'flex-end' },
  name: { fontSize: 22, fontWeight: '700' },
  score: { fontSize: 48, fontWeight: '800', marginVertical: 8 },
  badge: { fontSize: 14, color: '#2563EB', fontWeight: '600', marginBottom: 12 },
  warn: { color: '#F97316', marginBottom: 8 },
  desc: { marginTop: 16, lineHeight: 20, color: '#444' },
  meta: { marginTop: 12, color: '#888', fontSize: 13 },
  error: { color: '#EF4444', marginTop: 12 },
});
```

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/src/components/
git commit -m "feat(mobile): add spot detail sheet with hourly score chart"
```

---

### Task 19: EAS Build configuration for APK

**Files:**
- Create: `apps/mobile/eas.json`
- Modify: `apps/mobile/app.json`

- [ ] **Step 1: Create eas.json**

```json
{
  "cli": { "version": ">= 12.0.0" },
  "build": {
    "preview": {
      "android": {
        "buildType": "apk"
      },
      "env": {
        "EXPO_PUBLIC_SHOM_API_KEY": ""
      }
    }
  }
}
```

- [ ] **Step 2: Update app.json**

```json
{
  "expo": {
    "name": "Surf Pays Basque",
    "slug": "app-surf",
    "version": "1.0.0",
    "android": {
      "package": "com.poubone.surfpaysbasque",
      "config": {
        "googleMaps": {
          "apiKey": "YOUR_GOOGLE_MAPS_API_KEY"
        }
      }
    }
  }
}
```

- [ ] **Step 3: Document build in README.md**

```markdown
## Build APK

1. Obtenir une clé SHOM sur https://data.shom.fr
2. Copier `.env.example` → `.env` et renseigner `EXPO_PUBLIC_SHOM_API_KEY`
3. `npm run scrape && npm run copy-db`
4. `cd apps/mobile && npx eas build --platform android --profile preview`
```

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/eas.json apps/mobile/app.json README.md
git commit -m "chore: add EAS APK build configuration and README"
```

---

### Task 20: Final verification

- [ ] **Step 1: Run all unit tests**

Run: `npm test`
Expected: all scoring + scraper tests PASS

- [ ] **Step 2: Start Expo dev server (smoke test)**

Run: `cd apps/mobile && npx expo start`
Expected: Metro bundler starts without import errors

- [ ] **Step 3: Final commit if any fixes needed**

```bash
git commit -m "chore: final verification fixes"
```

---

## Spec Coverage Checklist

| Spec section | Task |
|---|---|
| Monorepo Expo + TS | Task 1, 12 |
| 12 spots Pays Basque | Task 8, 11 |
| Scraping 3 sources | Task 9, 10, 11 |
| SQLite embedded | Task 11, 15 |
| Open-Meteo API | Task 13 |
| SHOM API | Task 14 |
| Scoring algo /100 | Tasks 3–7 |
| Hendaye regression | Task 7 |
| Map colored pins | Task 17 |
| Hourly curve on tap | Task 18 |
| Network error screen | Task 18 |
| SHOM fail → score sans marée | Task 16 |
| APK via EAS | Task 19 |
| Stormglass v2 | Out of scope |
| Offline cache | Out of scope |

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-09-surf-app.md`. Two execution options:

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** — I execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
